
import 'package:flutter/material.dart';

class MDKSHome extends StatelessWidget {
  const MDKSHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('MDKS Home'),
      ),
    );
  }
}
